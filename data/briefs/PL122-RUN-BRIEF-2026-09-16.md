# PL 122 Eriugena run — shared stint brief (2026-09-16, Wilson's go: Opus, run through the whole thing)

Three stints run at once: **A** = 9176 chunks 0010–0012 · **B** = 9176 chunks 0013–0014 ·
**C** = the fragments 9175, 9178, 9181 (one chunk each). Your launch message says which you are.
Everything below binds all three.

## Read first, in this order
1. `translation-style.md`: the whole file, with particular care for **"Eriugena — register rules
   E1–E12"**, Pattern 7 / 7a (negation fidelity), and Patterns 11–13 and 15.
2. The style anchor pair: `src/latin/9176/0000.md` + `src/english/9176/0000.md`.
3. `src/english/9176/cruces.md`: the standing decisions and the merge section (vocabulary held
   across 0000–0009: Elias, Isaias, latchet, forerunner, Pasch, Holy Spirit, *theoria* italic,
   inhumanation / incarnation / humanation kept apart, *theorica scientia* "theoric knowledge").
   ⚠ Read it for CONVENTIONS only. It is not an answer key for your range.
4. `data/briefs/PL122-WITNESSES.md` (how to fetch both witnesses) and
   `data/briefs/PL122-VARIAE-LECTIONES-RULING.md` (Floss's apparatus).
5. **A and B:** your lemma brief, `data/briefs/9176-lemmata-0010.txt` (A) or `-0013.txt` (B).
   **Count the spans in your own Latin against the total printed at its head, and say so.**

## Output (English)
`src/english/<idno>/NNNN.md`, same filename as the Latin, **frontmatter copied VERBATIM**.

Sacred markers (breaking any one fails verification):
- Column anchors `[0473A]` / bare `[1137]`: VERBATIM, in order, in place. Count before writing.
- `[n: …]` verbatim and untranslated; a note that is editorial PROSE becomes a translated
  `[nt: …]` at the same position (Pattern 15). Same count, same order.
- `## ` heads translated 1:1; italics `*…*` and list structure preserved.
- English-only defect markers per Patterns 11–13 (`[d:]`, `[sic:]`, `[ed:]`), `[var:]` (7a″), `[cj:]`
  (Pattern 18). **PL 122: never a `[sic:]` resting on a single letter** (E12: sense + crux).
- **Floss's apparatus (new, live in code):** where the Latin carries `[vl: 1 | …]`, write NOTHING
  in the English (Latin-only). Where it carries `[fn: a | …]`, write a **translated `[nt: …]`** at the same
  point, ordered among any `[n:]`. Inside any marker write `(Hebrew)`, never `[Hebrew]`: no nested
  brackets. Your chunks carry none of these yet: YOU recover them (below).

Scripture: translate what the page prints, never a conventional version. **The divergence list is
the most valuable thing you produce**: walk every lemma and inline quotation word by word against
the Clementine (`sources/vulgate/clementine-flat.txt`) and fire `[var:]` only on a column you have
read at the plate.

**Negation fidelity (7a).** Every `non / nec / neque / nisi / haud / nunquam / nullus / nihil` the
Latin prints appears in the English, and none that it does not print, **including when carrying it
makes the sentence contradict itself.** Never translate a word as its opposite to rescue a sentence;
render as closely as the printed words allow and log the crux. A hidden repair is the one error
nobody downstream can find.

**E11, two printings.** Where the plate and our TEI differ, the English follows the reading that
CONSTRUES; the cruces record the other. File a TEI patch **only** where the plate is the sound
reading. Never file one toward a defective printing.

Em-dashes: rare; prefer comma, colon, full stop. Not a defect class; never report one.

**Known false positives, do NOT report:** column-band arithmetic (A–D are positional, not four per
column; only text broken mid-word across an anchor is real); an *Ibid.* resolving "wrong";
`noteCount` off by one; Pattern 4 `[f:]` in a lemma commentary; Migne's own `( sic )`; Pattern 9 items;
`(cont.)` heads.

## THE PLATE: read it inline, as you translate (runbook 4a″)
- **Working plate:** `https://archive.org/download/patrologiaecurs113unkngoog/page/n<LEAF>.jpg`
  (4309×6684). Map `leaf = (odd column + 73) / 2`, confirmed n70 → n210. **Read the printed corner
  numbers FIRST on every leaf** (C: your columns lie far past any calibrated leaf. Calibrate before
  reading anything, and if the map fails, step ±1–4 leaves until the corners match, and record it).
  Crop locally with PIL; **never let a crop cross the gutter**. Where the archive image clips a
  right column, read those line-ends on the second witness.
- **Second witness:** Google Books Princeton `YkVOAAAAYAAJ`, `pg=PA<odd column>` (signed-src
  fetch per the witnesses brief, fresh cookie jar per page). Word level only. **Never
  `q2mQHvy4_usC`**: it IS the archive photographs.
- ⛔ **Disk has ~1.8 GB free and two peers are fetching too.** Work in your scratchpad, one leaf at a
  time, and **delete each leaf and its crops as soon as you are done with it.**
- Collate the running text word by word against our Latin. A range read that way is `depth: "full"`.

## Floss's apparatus: recover it at the foot of every leaf you read
Below the text Floss prints **VARIAE LECTIONES** (raised numbers, restarting per page, notes like
`Cod. *ei*.`) and, below that, **NOTAE** (raised letters, restarting per page, prose, often
doctrinal or Jerome's etymologies with Hebrew). Crop the foot **full width**; a page either prints
the rule or it does not. A page without it is a **checked zero** and gets recorded as one.
- **Read where the raised key sits in the running text AT THE PLATE**, and write the anchor with the
  key cut in as `(1)` / `(a)`, e.g. `Quidquid gratia possideo (1), non aliunde`. ⚠ Our Latin
  italicizes lemmata: the anchor must reproduce our Latin's `*asterisks*` exactly as the chunk has
  them, or the injector will not find it. ⛔ **Never place a key by inference.** If you cannot see
  it, give the row `anchor_in_our_latin` = `no — key not visible …` and an empty anchor (at merge
  this session, the one inferred placement that was checked turned out to have no key there at all).
- Letters the bitonal scan cannot settle are **flagged, never guessed**: `Cod. *Ario[p?]agitam*.`
- Row format, **TAB-separated, 7 columns** (header exactly):
  `page	col	note_no	note_text	anchor_words	anchor_in_our_latin	layer`
  with `layer` = `vl` or `fn`, `page` = the archive leaf (`n202`), `col` = the band (`0330C`).
- **A: ⚑ the leaf n201 VL note ³ (`Cod. *nunquid*.`) lies in col 329C–330 and was never placed.
  Find its key.** n201 was otherwise already read by the 0007 stint (its keys ¹ and ² are placed);
  still read it yourself for 0329B onward.

## Files you write (stint-scoped: NEVER the shared files, which peers would race)
| what | A / B | C |
|---|---|---|
| English | `src/english/9176/NNNN.md` | `src/english/<idno>/0000.md` |
| cruces | `src/english/9176/cruces-<first>.md` | `src/english/<idno>/cruces.md` |
| plate reads | `data/briefs/9176-PLATE-READS-<first>.json` (same shape as `-0007.json`) | `data/briefs/<idno>-PLATE-READS.json` |
| Floss rows | `data/briefs/9176-PLATE-NOTES-<first>.tsv` | `data/plate-notes/<idno>.tsv` (per work, yours alone) |
| TEI divergences | `data/briefs/9176-PENDING-TEI-PATCHES-<first>.md` (find strings checked to match exactly once) | `data/briefs/<idno>-PENDING-TEI-PATCHES.md` |

⛔ **Do not** edit `data/plate-reads.json`, `data/plate-notes/9176.tsv`, `data/plate-notes/coverage.json`,
`data/tei-patches/*`, `sources/`, or any Latin chunk. **A and B: do not run `inject-plate-notes.mjs`** (it
rewrites every 9176 Latin chunk and your peer would race it). The orchestrator merges and injects.
**C may run `node scripts/inject-plate-notes.mjs <idno>` for its own three works**, then
`verify-english`, since no one else touches those directories. (It does write coverage.json. C is
the only stint allowed to, and only through the injector.)

## Verify before you finish
`node scripts/verify-english.mjs <idno>`. **A/B:** a failure on YOUR chunks is yours to fix,
EXCEPT `note markers — Latin N, English N+k`, where k = the `[nt:]` twins you wrote for your own
`fn` rows (their Latin `[fn:]` lands at merge). Name that expected k per chunk in your report. Chunks
outside your range will show MISSING; ignore them.
`node scripts/plate-gate.mjs <idno>` will not see your reads until merge; do not chase it.

## Report (final message)
Chunks written; spans counted vs the lemma brief; leaves read with corners; `[var:]` fired (column +
one line each); VL/NOTAE rows (placed / unplaced / checked zeros); TEI divergences filed and any
withdrawn under E11; the cruces that need a human, **named**; and **the chunks you found clean.**

Do not stop early. Peer stints finished three-chunk ranges with plates at full fidelity this same
day. If you catch yourself writing "context budget" at chunk 2, you are repeating a documented
failure: push through your whole range. A transport error is not a limit; the orchestrator will resume you.
